import { Router } from 'express';
// import * as studentSoftwareProgressController from '../controllers/studentSoftwareProgress.controller';

const router = Router();

// Routes will be added here when needed
// router.get('/', studentSoftwareProgressController.getStudentSoftwareProgress);

export default router;
